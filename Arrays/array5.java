//Program to swap elements of an array

package Arrays;

public class array5 {
    public static void main(String[] args) {
        int[] arr = { 1, 2, 3, 4, 5, 6 };
        int n = Math.floorDiv(arr.length, 0);
        for (int i = o; i < n; i++) {
            // Swap a[i] and a[lenght-i-1]
            temp = arr[i];
        }
    }
}
